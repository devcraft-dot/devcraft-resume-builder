# Google Sheet settings (edit before running fetch_sheet_urls.py)
#
# Spreadsheet ID from the URL:
# https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
spreadsheet_id=1lNXw3FFOL58tQ95UYqAF9NU897gH7hggbijULyA90W4

# Worksheet tab id (gid) from the URL hash: .../edit?gid=0
# Public sheets are fetched via CSV export — no Google account needed.
worksheet_gid=0

# Optional tab name (only used if worksheet_gid is blank)
# worksheet=Bid List

# First data row (1-based) on the FIRST run only. Later runs resume from fetch_state.json.
start_row=898

# Set true to ignore saved progress and start again from start_row above
reset_progress=false

# Progress file (relative to output_dir)
# state_file=fetch_state.json

# Column with job posting URL (Bid List sheet uses F = "Job URL")
url_column=F

# Optional metadata columns (leave blank to skip)
company_column=C
role_column=D
status_column=B

# Output directory (relative to this folder)
output_dir=output
